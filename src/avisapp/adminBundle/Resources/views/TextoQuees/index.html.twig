{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TextoQuees list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Texto</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_textoquees_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.texto }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_textoquees_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_textoquees_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_textoquees_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
